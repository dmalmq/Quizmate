Quiz Index Page
<% @quizzes.each do |quiz| %>
  <p>Quiz:<%= quiz.id %></p>
  <p>Number of question<%= quiz.number_of_question %></p>
  <p>Corrected:<%= quiz.corrected_times %></p>
  <p>Total points:<%= quiz.total_points %></p>
<% end %>
<%= link_to "Interest Index", interests_path %>
